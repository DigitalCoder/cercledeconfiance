{% extends 'AppBundle::layoutAppli.html.twig' %}

{% block title %}Bienvenue sur le Cercle{% endblock %}

{% block content %}

    <div class="container">
        <div class="row">
            <div class="col-xs-4">
                <a href="{{ path('accueil') }}">
                    <i class="fa fa-arrow-circle-o-left"></i> Retourner aux cercles
                </a>
            </div>
        </div>


        <div class="row text-center navAppli">
            <div class="col-xs-4">
                <a class="thumbnail navImage" href="{{ path('listMembers', {'token': token}) }}"
                   data-toggle="tooltip" data-placement="top" title="Gestion Membres">
                    <i class="fa fa-user-plus"></i>
                    <p class="navImageTitle">Gerer Membres</p>
                </a>
            </div>

            <div class="col-xs-4">
                <a class="thumbnail navImage" href="{{ path('edit_offer', {'token': token}) }}"
                   data-toggle="tooltip" data-placement="top" title="Gestion Cercle">
                    <i class="fa fa-check-circle-o"></i>
                    <p class="navImageTitle">Gerer Cercle</p>
                </a>
            </div>

            <div class="col-xs-4">
                <a class="thumbnail navImage" href="{{ path('objets', {'token': token}) }}"
                   data-toggle="tooltip" data-placement="top" title="Gestion Objets">
                    <i class="fa fa-video-camera"></i>
                    <p class="navImageTitle">Gerer les Objets</p>
                </a>
            </div>

            <div class="row">
                <div class="col-xs-4 col-xs-offset-4">
                    <a href="{{ path('appli', {'token': token}) }}">
                        <input type="button" class="btn btn-default" value="Retourner à l'appli"/>
                    </a>
                </div>
            </div>

            {#<a href="{{ path('listMembers', {'token': token}) }}">#}
                {#<input type="button" class="btn btn-success" value="Gerer Membres"/>#}
            {#</a>#}

            {#<a href="{{ path('edit_offer', {'token': token}) }}">#}
                {#<input type="button" class="btn btn-success" value="Gerer Cercle"/>#}
            {#</a>#}

            {#<a href="{{ path('objets', {'token': token}) }}">#}
                {#<input type="button" class="btn btn-success" value="Gerer Objets"/>#}
            {#</a>#}

        </div>
    </div>


{% endblock %}